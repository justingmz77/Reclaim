# Reclaim

**Reclaim** is a web application designed to help university students improve their mental health and wellness. It allows users to track their mood, build healthy habits, and access support resources — including those offered by the York Federation of Students (YFS).

---

## 🧠 Problem

University students often struggle to balance academics, work, and personal life — which leads to burnout and neglect of mental health. Many aren’t aware of the wellness support systems available to them, including those offered by their school.

---

## 🎯 Goal

Reclaim aims to help students:

- Reclaim their time, attention, and energy
- Track mental health and mood patterns
- Build consistent habits and wellness practices
- Access York Federation of Students (YFS) support resources

---


## 🚀 Getting Started

1. Clone the repo:
   ```bash
   git clone https://github.com/justingmz77/Reclaim.git
   cd Reclaim

2. Ensure that you have Node.js installed and `better-sqlite3`

   If not, you can use this [link](https://nodejs.org/en/download) to download Node.js and run `npm install better-sqlite3` to download `better-sqlite3`.
   
3. To start the web application, run `npm start` and open the application at the specified address outputted in your terminal.

---


## ⭐️ Useful Commands
- `npm start` to start the application
- `npm run seed` to seed and create the databases
- `npm run seed:fresh` to drop and re-seed the database
- `npm run dev` also runs the server
